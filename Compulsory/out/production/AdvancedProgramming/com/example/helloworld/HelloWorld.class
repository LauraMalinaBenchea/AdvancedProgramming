//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by FernFlower decompiler)
//

package com.example.helloworld;

public class HelloWorld {
    public HelloWorld() {
    }

    public static void main(String[] args) {
        System.out.println("Hello, World!");
        String[] languages = new String[]{"C", "C++", "C#", "Phyton", "Go", "Rust", "JavaScript", "PHP", "Swift", "Java"};
        int n = (int)(Math.random() * 1000000.0D);
        n *= 3;
        n += Integer.parseInt("10101", 2);
        n += Integer.parseInt("FF", 16);

        int sum;
        for(n *= 6; n >= 10; n = sum) {
            sum = 0;

            for(int variable = n; variable > 0; variable /= 10) {
                sum += variable % 10;
            }
        }

        System.out.print("Willy-nilly, this semester I will learn ");
        System.out.print(languages[n]);
    }
}
